/** The Lucid adapter (step 14). See `store/lucid_brand_theme_store.ts` for the implementation and its doc comment for why. */
export { LucidBrandThemeStore, type LucidBrandThemeStoreOptions } from './store/lucid_brand_theme_store.js';
